---
name: task-executor-eval
description: "Eval wrapper for the Task Executor agent. Implements a single task using TDD — writes tests first, confirms they fail, then implements code to make them pass. Use this when testing the executor's adherence to TDD discipline, completion record accuracy, and status signal correctness."
---

Read and follow the instructions in the task executor agent prompt. The agent prompt is located at the path provided in the task prompt.
