{
  "skill_name": "task-executor-eval",
  "evals": [
    {
      "id": 1,
      "prompt": "You are the Task Executor. Read the agent prompt at `.planning/agents/task-executor.md`. Your task file content is in `.planning/evals/fixtures/sample-task.json`. The manifest is at `.planning/evals/fixtures/sample-manifest.json`. This is iteration 1 (first attempt). Implement the task following TDD. The project uses Next.js with Drizzle ORM.",
      "expected_output": "Test files and implementation files are both created. The task file's test_files array is populated. A completion record is written to the manifest with all required fields. All declared files exist on disk.",
      "files": [],
      "expectations": [
        "Test files exist on disk covering both scenarios (SC-01 bookmark creation and SC-02 duplicate rejection)",
        "The task file's test_files array is updated with the paths of created test files (not left as an empty array)",
        "A completion record is written to the manifest entry for T01 with status set to complete",
        "The completion record includes summary, matched_plan, drift_type, and broken_assumptions fields",
        "All declared files from the task's files array are created on disk (route.ts, BookmarkForm.tsx, BookmarkList.tsx, schema.ts)",
        "Test files are separate from implementation files — tests are not inline in the source files"
      ]
    },
    {
      "id": 2,
      "prompt": "You are the Task Executor. Read the agent prompt at `.planning/agents/task-executor.md`. Your task file content is in `.planning/evals/fixtures/sample-task.json`. The manifest is at `.planning/evals/fixtures/sample-manifest.json`. This is iteration 1. The task specifies POST /api/bookmarks returns 201. However, you discover during implementation that the Drizzle ORM returns snake_case field names (folder_id instead of folderId). Implement the task, noting any drift accurately.",
      "expected_output": "The executor should complete the task but report structural drift — the assumption A01 specifies camelCase 'folderId' but the ORM returns 'folder_id'. The completion record should accurately describe this as structural drift with the broken assumption identified.",
      "files": [],
      "expectations": [
        "The completion record has matched_plan set to false",
        "The drift_type is 'structural' (not 'local' — the API response shape is an external surface other tasks depend on)",
        "The broken_assumptions array references assumption A01 by id",
        "The broken_assumptions entry describes the reality (snake_case field names from Drizzle ORM)",
        "The executor does NOT stop or fail because of the drift — it completes the task and records the deviation"
      ]
    },
    {
      "id": 3,
      "prompt": "You are the Task Executor. Read the agent prompt at `.planning/agents/task-executor.md`. Your task file content is in `.planning/evals/fixtures/sample-task.json`. This is iteration 2.\n\nBefore starting the executor workflow, set up the iteration 1 state:\n1. Create src/__tests__/bookmarks.test.ts with tests for SC-01 (POST /api/bookmarks returns 201 with bookmark object) and SC-02 (POST /api/bookmarks returns 409 for duplicate URL). Tests should use a standard test framework.\n2. Create src/app/api/bookmarks/route.ts with a basic POST handler stub that returns 201 but does not yet implement duplicate detection.\n3. Do NOT create BookmarkForm.tsx or BookmarkList.tsx — those are the missing pieces.\n\nResumption context: iteration 1 was attempted. Test files exist at src/__tests__/bookmarks.test.ts and are correctly written. Tests are currently failing because the implementation was not completed — only the API route exists, the React components were not created. Pick up where the previous iteration left off.",
      "expected_output": "The executor should preserve existing test files, determine implementation is incomplete, and continue from where iteration 1 stopped by creating the missing React components.",
      "files": [],
      "expectations": [
        "The pre-existing test file at src/__tests__/bookmarks.test.ts is not deleted and replaced with a completely different file",
        "The missing React components (BookmarkForm.tsx and BookmarkList.tsx) are created",
        "A completion record is written to the manifest with status complete",
        "The implementation satisfies the scenarios — both bookmark creation and duplicate rejection are handled"
      ]
    }
  ]
}
